"use client";

import AuthForm from "@/components/common/AuthForm";
import React from "react";

const page = () => {
  return (
    <div>
      <AuthForm type="login" />
    </div>
  );
};

export default page;
